import Head from "next/head";

export default function Page({ repo }) {
    return (
         <>
            <Head>
                <title>Login</title>
            </Head>
            <div>Page login</div>
         </>
    )
    
}